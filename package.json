{
  "name": "obsidian-tv-show-search",
  "version": "1.0.0",
  "description": "Search TV shows via TMDB and save details as markdown files in Obsidian with frontmatter",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "deploy": "source .env && cp main.js manifest.json styles.css versions.json \"$DEPLOY_DESTINATION\""
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.5.0",
    "tslib": "^2.6.0",
    "typescript": "^5.3.0"
  }
}
